//BSYHC - Base SisYpH Compiler
python3 bshyc.py -i(входной файл) b.syh -c(означает скомпилировать) -py(означает скомпилировать в python3) -o(выходной файл) bsyhc-test-b.py
python3 bsyhc.py -i(входной файл) bsyhc-test-b.py -d(декомпилировать) -o(выходной файл) b-de.syh
python3 bsyhc.py -i (входной файл) //Если просто без флагов вот так это значит интерпретировать
python3 bshyc.py -i (входной_файл) -c -elf(означает скомпилировать в elf через pyinstaller) -o (имя выходного файла)
python3 bshyc.py -i (входной_файл) -c -exe(означает скомпилировать в exe через pyinstaller) -o (имя выходного файла)
//Компилятор будет работать так:он упаковает .syh файл в массив где новая строка - новый элемент массива.
//Дальше мы вместо строк из файла подаём на вход интерпритатору файла наш сгенерированный массив
//ну и дальше просто запускаем этот код
//И такой скомпилированный файл можно легко декомпилировать
